# GitHub 热门项目获取工具依赖列表
# Python 版本要求: >= 3.7

# 核心依赖
requests>=2.25.1        # HTTP 请求库
urllib3>=1.26.0         # HTTP 客户端

# 可选依赖 (用于增强功能)
python-dateutil>=2.8.0  # 日期处理
pytz>=2021.1            # 时区处理
markdown>=3.3.0         # Markdown 处理 (如需转换HTML)

# 开发依赖 (可选)
pytest>=6.0.0           # 单元测试
pytest-cov>=2.10.0      # 测试覆盖率
black>=21.0.0           # 代码格式化
flake8>=3.8.0           # 代码检查

# 部署依赖 (可选)
gunicorn>=20.0.0        # WSGI 服务器 (如果要做 Web 服务)
schedule>=1.1.0         # Python 定时任务 (替代 cron)
